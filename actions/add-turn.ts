'use server'

// import { addTurnToDebate } from '@/data/debate'

export async function addTurn(data: FormData, debateId: string) {
  /*const response = await addTurnToDebate(data, debateId);
  return response;*/
  return null;
}